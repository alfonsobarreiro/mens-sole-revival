/** Limits shared by the /ask page and the /api/ask route. */

/** Longest question a visitor can send. */
export const MAX_INPUT_CHARS = 500;

/** Questions allowed in one conversation before the visitor must start over. */
export const MAX_TURNS = 10;
