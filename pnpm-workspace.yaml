# Which dependencies may run install scripts.
# true  = needs its script to fetch or verify a native binary
# false = script is only a banner or version check
allowBuilds:
  core-js: false
  esbuild: true
  protobufjs: false
  sharp: true
  unrs-resolver: true
